export interface BBResizeOptions {
  pane: {
    knob: {
      constantlyShow: boolean;
    };
  };
}

export interface BBResizeStyles {
  cursor: {
    active: {
      horizontal: string;
      vertical: string;
    };
  };
}
