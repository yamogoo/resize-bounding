import { createApp } from "vue";
import App from "@/App.vue";

// import Vue3BbResize from "vue3-bb-resize";

const app = createApp(App);
// app.use(Vue3BbResize, { name: "resize" });
app.mount("#app");
